# Core dependencies for SparseLoRA (spft)
# Based on https://github.com/z-lab/sparselora/blob/main/pyproject.toml

# Install spft from GitHub (use editable install from cloned repo)
# First clone: git clone https://github.com/z-lab/sparselora.git sparselora_repo
# Then: pip install -e sparselora_repo/

# Core ML dependencies
torch>=2.5.1
transformers>=4.46.3
peft>=0.13.2
accelerate>=1.1.1
datasets>=3.1.0
evaluate>=0.0.0

# Utilities
numpy>=2.1.3
pyyaml>=6.0.2
tqdm>=4.67.0
safetensors>=0.4.5
huggingface-hub>=0.26.2
tokenizers>=0.20.3

# Required dependencies
wandb>=0.18.7
bitsandbytes>=0.42.0
colorama>=0.4.6
scikit-learn>=1.0.0
ipywidgets>=8.0.0

# Note: liger-kernel is optional on macOS (patched to be optional)
# It requires triton which is CUDA-only, so it won't work on macOS

